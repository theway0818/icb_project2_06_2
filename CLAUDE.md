# 프로젝트 작업 규칙

## 기본 원칙

1. 작업은 프로젝트 루트를 기준으로 수행한다.
2. 데이터 분석은 노트북이 아니라 Python 파일로 작성한다.
3. 원본 데이터는 `data/raw/`에서 읽고 수정하거나 덮어쓰지 않는다.
4. 가공 데이터가 필요하면 `data/processed/`에 별도로 저장한다.
5. 분석 코드는 `analysis/`, 표는 `outputs/tables/`, 그래프는 `outputs/figures/`에 저장한다.
6. 파일 경로는 프로젝트 내부의 상대경로를 사용한다.
7. 실행에 필요한 라이브러리는 코드 상단에 명시하고 같은 입력에서 다시 실행할 수 있게 작성한다.
8. 원본에 없는 값이나 결과를 임의로 만들지 않는다.
9. 오류가 발생하면 원인을 확인하고 기존 원본 파일을 변경하지 않는 범위에서 수정한다.
10. 요청하지 않은 Markdown 안내 문서나 중간 파일을 만들지 않는다.

## 데이터 보호

1. 파일을 사용하기 전에 실제 파일명과 열 이름을 확인한다.
2. 행을 제외하거나 값을 대체하면 적용한 기준과 건수를 결과에 남긴다.
3. 식별자는 분석용 수치변수로 사용하지 않는다.
4. 주문 이후에 생성된 정보가 예측 입력에 포함되지 않도록 확인한다.
